[workspace]
resolver = "3"
# members = [ "cube_core", "cube_cli", "cube_methods" ]
members = [ "cube_core" ]

[profile.release]
opt-level = 3
lto = true
codegen-units = 1
panic = "abort"
strip = "symbols"

[workspace.dependencies]
